/**
 * The 'gen' folder contains all generated code for the domain.
 * Outside of the 'gen' folder we can have 'rule' folder for
 * those rules that are inter-dependent with the domain code (e.g. rule provider).
 */
package zhy2002.examples.lodgement;